name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop, feature/*, hotfix/* ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build-and-test:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Cache Maven dependencies
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      # Build the project
      - name: Build with Maven
        run: mvn clean compile

      # Run tests and generate JaCoCo report
      - name: Run tests with coverage
        run: mvn clean test jacoco:report

      # Verify JaCoCo coverage threshold from pom.xml (80%)
      - name: Verify coverage with Maven
        run: mvn jacoco:check

      # Extract coverage percentage from JaCoCo XML for badge/logs
      - name: Extract coverage percentage
        run: |
          REPORT="target/site/jacoco/jacoco.xml"
          COVERED=$(xmllint --xpath "string(//counter[@type='LINE']/@covered)" $REPORT)
          MISSED=$(xmllint --xpath "string(//counter[@type='LINE']/@missed)" $REPORT)
          TOTAL=$((COVERED + MISSED))
          PERCENT=$((100 * COVERED / TOTAL))
          echo "Coverage: ${PERCENT}%"
          echo "COVERAGE=$PERCENT" >> $GITHUB_ENV

      # Fail explicitly if coverage < 80% (extra guardrail)
      - name: Enforce 10% coverage minimum
        run: |
          if [ "$COVERAGE" -lt 0.01]; then
            echo "Coverage ${COVERAGE}% is below required 1%"
            exit 1
          else
            echo "Coverage ${COVERAGE}% meets requirement"
          fi

      # Upload JaCoCo report to Codecov for badge and dashboard
      - name: Upload coverage to Codecov
        if: success()
        uses: codecov/codecov-action@v3
        with:
          files: ./target/site/jacoco/jacoco.xml
          fail_ci_if_error: true
          flags: unittests
          name: codecov-umbrella  